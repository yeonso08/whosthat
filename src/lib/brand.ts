/**
 * 브랜드 이름의 조각.
 *
 * 워드마크는 `@whosthat`, 마크는 그 앞의 `@` 하나다. 헤더 워드마크와
 * 파비콘·앱 아이콘이 같은 글자를 그리므로 한 곳에 둔다.
 */

/** 파비콘과 앱 아이콘이 그리는 마크. 워드마크 앞의 `@` 와 같은 글자다. */
export const BRAND_MARK = "@";

/** 워드마크의 이름 부분. 도메인이자 브랜드라 한글로 옮기지 않는다. */
export const BRAND_WORDMARK = "whosthat";
