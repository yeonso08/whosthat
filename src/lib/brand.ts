/**
 * 브랜드 이름의 조각.
 *
 * 워드마크는 `[ㄲ 마크] 누꼬`(ko)·`[ㄲ 마크] nukko`(en·ja) — 마크는 겹친 두 원에서
 * ㄱ 두 개를 깎은 모노그램(ㄲ)이다. 헤더 워드마크와 파비콘·앱 아이콘이 같은
 * 도형을 그리므로 한 곳에 둔다.
 */

import type { Locale } from "./locales";

/**
 * 파비콘·앱 아이콘·워드마크가 함께 그리는 마크의 좌표.
 *
 * 겹친 두 원(얼굴·계정을 잇는 모양)의 둘레를 따라 ㄱ 을 하나씩 깎으면 ㄲ 이
 * 된다 — 누꼬의 ㄲ, nukko 의 kk. 한국어 사용자에게는 글자로, 그 밖에는 나란한
 * 두 훅으로 읽히는 이중 독해가 이 마크의 자산이다.
 *
 * **언어를 타지 않는다.** `icon.tsx`·`apple-icon.tsx` 는 `[lang]` 바깥이라
 * 애초에 언어를 못 받고, 언어들이 이 도형을 공유하는 이유이기도 하다.
 *
 * `stroke-linecap: round` 로 그리므로 획 끝이 사이트의 반경 규칙(12–16px)과
 * 같은 태도를 공유한다 — 각진 ㄱ 대신 이 모양을 고른 이유다.
 */
export const BRAND_MARK_VIEWBOX = "0 0 88 64";
export const BRAND_MARK_PATHS = [
  "M8 14 H26 A18 18 0 0 1 44 32 V50",
  "M44 14 H62 A18 18 0 0 1 80 32 V50",
];

/** 마크의 가로:세로 비율. 아이콘 라우트가 폭만 정하고 높이를 이 값으로 뺀다. */
export const BRAND_MARK_ASPECT = 88 / 64;

/**
 * 워드마크의 이름 부분.
 *
 * `누꼬` 는 `nukko` 의 번역이 아니라 **원문**이라 언어를 따라간다 — 한국어 화면이
 * 원문 대신 로마자 표기를 쓸 이유가 없다. (`whosthat` 시절 한글 워드마크를 반려한
 * 건 그때는 그게 번역이었기 때문이다. 이름이 사투리로 바뀌면서 조건이 뒤집혔다.)
 *
 * **그 예외는 한국어에서만 성립한다.** 일본어가 `ヌッコ` 가 아니라 `nukko` 인
 * 이유다 — 가타카나로 옮기면 그건 원문이 아니라 음역이고, "브랜드를 번역하지
 * 않는다" 는 규칙을 언어마다 하나씩 깨는 셈이 된다. 가명을 가타카나로 적는
 * 것과 다른 문제다: 가명은 일본어 문장 안에서 읽히는 **데이터**고, 이건 도메인
 * (`nukko.net`)과 짝을 이루는 고유명이다.
 */
export const BRAND_WORDMARK: Record<Locale, string> = {
  ko: "누꼬",
  en: "nukko",
  ja: "nukko",
};

/**
 * 이름 부분에 붙일 서체. 라틴 이름만 `font-lat`(Manrope)이고 한글은 본문
 * 서체(Gothic A1)를 쓴다 — 프로젝트 규칙(라틴·숫자는 `font-lat`) 그대로다.
 *
 * 마크는 이제 글자가 아니라 도형(SVG)이라 서체를 안 탄다 — 이 표는 이름에만
 * 적용된다. 한글 쪽을 빈 문자열이 아니라 `font-sans` 로 못박은 건 이 값이
 * `font-lat` 을 이미 걸어 둔 상자 안(푸터 카피라이트 줄)에도 들어가기 때문이다.
 */
export const BRAND_WORDMARK_FONT: Record<Locale, string> = {
  ko: "font-sans",
  en: "font-lat",
  ja: "font-lat",
};

/**
 * 이름 부분의 세로 광학 보정.
 *
 * `items-center` 는 마크와 이름의 **줄 상자**를 정확히 맞춘다 — 실측해 보면
 * 두 상자의 높이·중심이 픽셀 단위로 같다. 어긋나는 건 그 상자 안에서 글자가
 * 실제로 잉크를 칠하는 위치다: Gothic A1 의 한글 글리프는 Manrope 의 라틴
 * 소문자보다 베이스라인 아래쪽으로 덜 내려간다(획순 구조상 받침·모음이
 * 위쪽에 몰린다). 그래서 `누꼬` 는 상자 중심보다 살짝 위에서 잉크가 맺히고,
 * 마크의 광학 중심과 어긋나 보인다 — 헤더 크기(15px)에서 약 1.4px, 푸터
 * 크기(11px)에서 약 0.8px, 실측치.
 *
 * 고정 px 대신 `em` 인 이유는 이 값이 헤더(`Wordmark`)와 푸터(`SiteFooter`)
 * 두 자리에서 서로 다른 글자 크기로 쓰이기 때문이다 — 비율로 두면 크기가
 * 바뀌어도 같은 보정이 먹는다. 영어·일본어는 둘 다 라틴 이름(`nukko`)이라
 * 보정이 필요 없다.
 */
export const BRAND_WORDMARK_NUDGE: Record<Locale, string> = {
  ko: "translate-y-[0.1em]",
  en: "",
  ja: "",
};

/**
 * 이미지 라우트(파비콘·앱 아이콘·OG 카드)가 쓰는 색.
 *
 * satori(`ImageResponse`)는 CSS 를 실행하지 않아서 `globals.css` 의 토큰을 못
 * 읽는다 — 값으로 박는 수밖에 없다. 다크 팔레트의 `--background`·`--foreground`·
 * `--muted-foreground` 와 같은 값이고, **한쪽만 고치면 어긋난 채로 아무 에러도
 * 안 난다.** 공유 카드는 늘 어두운 판으로 그린다(라이트 모드에서도 그렇다) —
 * 카드가 뜨는 자리는 방문자의 테마를 모른다.
 */
export const BRAND_IMAGE_COLORS = {
  background: "#0a0a0c",
  foreground: "#f4f4f6",
  muted: "#90909b",
};
