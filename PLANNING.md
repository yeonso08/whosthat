# 프로젝트 기획: 예능 출연진 인스타그램 아카이브

## 1. 배경 및 목적

"나는 솔로" 등 예능 프로그램 출연진의 인스타그램 계정을 찾으려면 매번 검색 → 블로그/커뮤니티 글 확인 → 계정이 없거나 틀린 경우 재검색을 반복해야 한다. 이 과정을 한 번에 해결하는 아카이브 사이트를 만든다.

- **1차 목표**: 나는솔로 전 기수 출연진의 인스타그램 계정을 정리해 검색 없이 바로 찾을 수 있게 한다.
- **장기 목표**: 넷플릭스 등 다른 프로그램으로 범위를 넓혀, 프로그램별 출연진 인스타 아카이브 플랫폼으로 확장한다.

## 2. 타겟 사용자

- 특정 프로그램을 보고 "이 사람 인스타 뭐지?" 하고 검색하는 일반 시청자
- 특정 기수/시즌 출연진을 한눈에 훑어보고 싶은 팬

## 3. MVP 범위

- **대상**: 나는솔로 전 기수 (1기 ~ 최신 기수)
- **핵심 기능**
  - 기수별로 출연진 목록 보기
  - 출연진 카드(사진, 이름, 나이/직업 등 기본 정보, 인스타 링크 — 사진 리스크는 §9 ①)
  - 기수/이름 검색 및 필터
- **MVP에서 제외 (이후 단계)**
  - 사용자 제보/기여 기능
  - 나는솔로 외 프로그램
  - 로그인/계정 시스템

## 4. 데이터 수집 방식

- **Phase 1 (MVP)**: 직접 수동으로 검색해 데이터 파일(JSON 등)에 입력. 초기엔 정확도와 관리 편의성이 우선.
- **Phase 2**: 사용자 제보/수정 제안 기능 추가
  - 시청자가 "이 계정 아니에요", "새 계정 찾았어요" 같은 제보를 남길 수 있는 폼
  - 제보는 검수 후 반영 (바로 반영 X — 잘못된 정보/장난 계정 방지)
- 모든 데이터 항목에 `lastVerified`(마지막 확인일), `source`(확인 경로) 필드를 두어 정보 신뢰도를 관리한다.

## 5. 데이터 모델 (초안)

```
Program (프로그램)
  - id
  - name              예: "나는 솔로"
  - type              예능 / 드라마 / 서바이벌 ...
  - platform           방송사/OTT (SBS Plus, Netflix 등)

Season (기수/시즌)
  - id
  - programId
  - number            기수·시즌 번호 (라벨은 저장하지 않는다 — 아래)
  - airDate

CastMember (출연진)
  - id
  - seasonId
  - alias              방송에서 불린 이름 — 항상 있다 (가명이거나 실명이다, 아래)
  - name               실명 (공개된 경우에만)
  - occupation        직업 (선택)
  - ageAtAiring        방영 당시 나이 (선택)
  - instagramHandle    "@" 없이 핸들만 (status가 found일 때만)
  - status             found / none / searching
  - lastVerified       마지막 확인 날짜
  - source             확인한 출처 (직접 검색 / 제보 등)

Submission (제보 — Phase 2)
  - id
  - castMemberId (기존 수정 제안) 또는 신규 출연진 정보
  - proposedHandle
  - status             pending / approved / rejected
  - createdAt
```

### `verified` 불린 → `status` 3상태 (구현하며 변경)

불린으로 두면 "찾아봤는데 계정이 없더라"와 "아직 안 찾아봤다"가 똑같이 `false`가 된다. 그러면 방문자가 이미 확인이 끝난 사람을 또 검색하게 되는데, **그 헛수고를 없애는 게 이 사이트를 만드는 이유**였다. 그래서 셋으로 나눈다.

| status | 뜻 | 화면 |
|---|---|---|
| `found` | 계정을 찾아서 확인까지 마쳤다 | 카드가 밝아지고 인스타로 링크 |
| `none` | 찾아봤고, 계정이 없다는 것까지 확인했다 | "계정 없음 · 확인일" |
| `searching` | 아직 못 찾았다 (제보 받는 상태) | "찾는 중" (유일한 유채색) |

`found`는 `lastVerified`와 `source`를 반드시 함께 채운다. 그 두 줄이 이 사이트를 믿을 근거다.

### `profileImageUrl` — 뺐다가 되돌렸다 (구현하며 변경)

§9 ① 의 초상권·저작권 우려로 2026-08-19 에 필드를 통째로 없앴다가, **2026-08-20 에 다시 넣었다**(§8 디자인도 함께 되돌렸다).

되돌리면서 달라진 점은 **필드가 선택이라는 것**이다. 값이 없으면 화면이 깨지지 않고 가명 배지(`CastAvatar`)가 그 자리를 채운다. 즉 "사진을 쓸 수 있는 자리"와 "그 자리에 실제로 무엇을 올리느냐"가 분리돼 있다 — 전자는 열려 있고, 후자는 §9 ① 의 판단을 사람이 건당으로 한다. 값은 `/public` 아래 경로만 쓴다(핫링크 금지, §9 ⑤).

### `label` → `number`, `alias` 의 뜻이 넓어졌다 (프로그램 확장하며 변경, 2026-08-31)

솔로지옥을 붙이며 두 필드가 나는 솔로 전용이었다는 게 드러났다(§8-2).

- **`label`("20기")을 `number`(20)로 바꿨다.** 프로그램마다 부르는 말이 다르다 — `33기` 와 `시즌 4`. 라벨을 데이터에 저장하면 언어 × 프로그램만큼 적어야 해서, 번호만 두고 사전이 언어별로 만든다.
- **`alias` 는 "방송 가명"이 아니라 "방송에서 불린 이름"이다.** 나는 솔로는 가명(`영수`), 솔로지옥은 실명(`최시훈`)이 들어간다. 필드를 나누지 않은 건 화면이 하는 일이 같아서다 — 크게 부르는 이름 하나와 그 아래 실명·나이·직업 줄. 실명이 들어가는 프로그램은 `name`(실명)을 비워 둔다.

프로그램 확장을 고려해 **Program → Season → CastMember** 3단 구조로 설계한다. 나는솔로처럼 "기수" 개념이 있는 프로그램뿐 아니라, 시즌 개념이 없는 단발성 프로그램도 다룰 수 있도록 `Season`은 프로그램마다 최소 1개는 있는 것으로 취급한다(예: 시즌 없는 프로그램은 "기본 시즌" 하나로 처리).

## 6. 향후 로드맵

1. 나는솔로 전 기수 MVP 출시
2. 사용자 제보 기능 추가
3. ~~프로그램 확장~~ (구조는 구현됨 — §8-2. 솔로지옥이 첫 확장이고, 명단·계정은 채우는 중)
4. ~~검색~~ (구현됨 — §10) → 필터 고도화 (인스타 팔로워 수, 최근 활동 등은 범위 밖 — 계정 정보 큐레이션에 집중)

## 7. 기술 스택

- **프레임워크**: Next.js (React) — 향후 프로그램/출연진 페이지의 검색엔진 노출(SEO)과 제보 기능용 API 라우트를 고려해 선택
- **스타일링**: Tailwind CSS (shadcn 등)
- **데이터 저장**: MVP는 정적 JSON/파일 기반으로 시작 → 제보 기능 추가 시 DB 도입 검토

### DB·백엔드는 언제 필요한가

지금은 필요 없다. **읽기만 하는 사이트는 DB 가 없을 때가 제일 빠르다** — 전 페이지가 SSG 라 방문자 요청에 도는 서버가 아예 없다.

**데이터가 늘었다는 건 트리거가 아니다.** 408명짜리 JSON 이 112KB 고, 만 명이 돼도 SSG 는 멀쩡하다. 검색 기능(로드맵 4)도 마찬가지 — 전체 데이터가 애초에 작아서 클라이언트에서 필터링하면 끝이다. 프로그램이 늘어난다는 이유만으로 옮기지 말 것.

**진짜 트리거는 쓰기가 생길 때다.**

1. **제보 폼**(로드맵 2) — 방문자가 데이터를 넣는 첫 기능이라 JSON+git 으로는 못 받는다. 다만 곧장 DB 로 갈 필요는 없다. 하루 몇 건이면 `폼 → 서버리스 함수 → 메일` 이 먼저고, 메일함이 사실상 큐 노릇을 한다. **그 정리가 손에 부칠 때**가 DB 시점이다.
2. **삭제·정정 요청 지연** — §9 와 `/takedown` 이 "즉시 반영"을 약속해 뒀는데 지금 절차는 JSON 수정 → 커밋 → 재배포다. 현재 빈도면 몇 분이라 괜찮지만, 요청이 쌓여 대기가 생기면 그건 약속 위반이라 우선순위가 올라간다.
3. **관리자 편집 화면** — 프로그램이 늘어 JSON 손편집이 병목이 될 때.
4. **사용자별 상태**(로그인·즐겨찾기) — 현재 로드맵엔 없다.

**그 사이의 중간 단계.** DB 없이 되고, 나중에 옮길 때 그대로 스키마 제약이 된다.

- JSON 을 프로그램 단위로 쪼개기
- 빌드 타임 검증 스크립트 — 핸들 형식, `found` 인데 `source`·`lastVerified` 가 빈 경우

**옮길 때 고칠 파일은 `lib/data.ts` 하나다.** JSON 을 직접 import 하는 파일을 거기 하나로 묶어 둔 게(`CLAUDE.md` 의 의존 방향 규칙) 정확히 이걸 위한 장치다. 그 규칙을 지키는 한 이전 비용은 계속 작게 유지된다.

## 8. 디자인 방향 — 시안 D "어둠 속 사진" (확정)

시안 5종을 거쳐 D로 확정했다. 구현 규칙은 `CLAUDE.md`, 시안 원본은 [디자인 캔버스](https://claude.ai/code/artifact/1e0af2e1-9bf1-495e-81a3-961765ac044e)의 "새 시안" 페이지. 사진을 한 번 걷어냈다가 2026-08-20 에 되돌렸다.

- **사진 자리는 세 곳이다.** 상세의 2열 사진 카드 그리드, 목록 히어로에 겹쳐 쌓는 원 6개, 목록 줄에 겹쳐 쌓는 작은 얼굴 원 4개.
- **히어로는 2026-08-21 에 3장 사진 스트립에서 겹친 원 줄로 바꿨다.** 사진이 한 장도 없는 동안 스트립이 정보량 0인 가명 상자 셋이 돼 기수 이름과 현황을 아래로 밀었다. 값은 히어로와 목록 줄의 그림이 비슷해진 것.
- **사진이 없는 사람은 가명 두 글자 배지가 그 자리를 채운다.** 저대비 워터마크로 둔다 — 상태색을 그대로 칠했더니 히어로 타일이 색면 띠가 돼서 낮췄다(아래 반려 방향 참고).
- **색은 흑백뿐**이고, 유일한 유채색(황토)은 `searching` 상태 전용이다.
- **라이트·다크 두 팔레트를 다 가진다**(2026-08-20, "다크 전용" 원칙 폐기). 명도만 뒤집었지 구조는 같다. 첫인상은 원래 시안대로 다크가 기본이고, 전환 버튼은 헤더 워드마크 옆에 있다.
- **모바일 우선이되 데스크톱을 따로 짠다**(2026-08-31). 그전에는 `body` 가 640px 고정이라 큰 화면이 가운데 좁은 기둥 하나였다 — 프로그램이 늘면서 그 폭이 통째로 안 맞게 됐다. 컨테이너를 1280px 까지 열고, 격자를 2→5열로 늘리고, 여백을 유틸리티 한 쌍으로 모았다. 좁은 화면에서는 새 값이 안 걸려 모바일 화면은 그대로다. 산문(정책 페이지)과 검색창만 따로 폭을 묶는다 — 컨테이너를 다 쓰면 읽을 수 없거나 빈 띠가 된다.

반려된 방향(참고): 색면 포스터 계열 — 시골 옥색 명찰, 다크 플럼 번지 숫자, 블록 초록. 굵은 디스플레이 서체 + 강한 색면 + 거대 숫자 조합은 이 프로젝트에 맞지 않는다. 사진이 빠진 자리를 이걸로 메우고 싶어지기 쉬운데, 그 유혹도 마찬가지로 반려 대상이다.

## 8-1. 언어 — 글로벌 확장 (2026-08-21, 일본어 2026-08-25)

한국 예능 출연진을 해외에서도 찾는다는 판단으로 언어를 붙였다. 1차는 영어, 2차는 일본어다. 구현 규칙은 `CLAUDE.md` 의 "언어" 절.

- **URL 이 언어를 정한다**(`/ko`·`/en`·`/ja`). 언어 없는 옛 주소는 한국어로 308, `/` 만 브라우저 언어를 보고 307 한다.
- **번역 대상은 셋이다**: 화면 문구(사전 JSON), 데이터 어휘(가명 21개·특집 13개·프로그램 이름), 날짜·현황 포맷. 출연진 408명을 손으로 번역할 일은 없다 — 가명이 21개뿐이라 표 하나로 덮인다.
- **가명은 그 언어의 표기로 적는다**(`영수` → `Yeongsu`·`ヨンス`). 다만 검색은 한글도 받는다. 사진 없는 자리의 배지만 한글을 그대로 쓰는데, 이유는 `CLAUDE.md` 에 적어 뒀다.
- **표기가 사람을 못 가르면 표기를 바꾼다.** 일본어에서 `정수`·`종수` 가 관행대로면 둘 다 `ジョンス` 인데 1·2·3기는 그 둘이 같은 기수에 있다 — 한 화면에 이름이 같은 사람이 둘 생기면 이 사이트의 존재 이유가 무너지므로, 실제로 둘 다 쓰이는 표기 중에서 갈라 못박았다.
- **서체가 언어를 따라간다.** 한글 폰트에는 가나·한자가, 일본어 폰트에는 한글이 없어서 한 벌로 못 쓴다. 화면과 OG 이미지가 같은 표를 보고 언어마다 하나씩 고른다.
- **정책 두 페이지의 번역본은 원문이 아니고 원문은 한국어다**(화면에 명시). 삭제 요청 창구가 언어별로 다른 약속을 하면 §9 의 사후 대응 방침이 무너진다.
- **워드마크는 한국어만 원문이다**(`누꼬`). 영어·일본어는 도메인과 같은 `nukko` 다 — 가타카나로 옮기면 그건 원문이 아니라 음역이다.
- 언어를 늘려도 데이터는 하나다. 계정·사진 방침, 삭제 요청 처리는 언어와 무관하게 같은 절차다.

## 8-2. 프로그램 확장 — 솔로지옥 (2026-08-31)

§1 의 장기 목표이자 로드맵 3단계. 나는 솔로 하나만 있던 화면을 프로그램 단위로 열고, 넷플릭스 「솔로지옥」을 두 번째 프로그램으로 붙였다. 구현 규칙은 `CLAUDE.md` 의 "프로그램" 절.

- **화면이 세 단계가 됐다**: 홈(프로그램 목록) → 프로그램(기수/시즌 목록) → 기수 상세. 그전에는 홈이 곧 나는 솔로의 기수 목록이었다.
- **주소에 프로그램이 들어간다**(`/ko/i-am-solo/seasons/s33`). **슬러그는 공식 영어 제목이다** — 한국어 로마자로 시작했다가 바꿨다. 주소는 언어별로 나뉘지 않으니 언어를 안 타는 표기라야 하고, 프로그램마다 규칙이 갈리면 다음 프로그램에서 또 고민하게 된다. 기수 `id` 는 프로그램 안에서만 고유해서(`s1` 이 두 프로그램에 다 있다) 프로그램 없이는 어느 기수인지 정해지지 않는다. **옛 주소는 308 로 넘긴다** — 이미 색인·공유된 링크다. 언어가 붙기 전(`/seasons/s33`)과 프로그램이 붙기 전(`/ko/seasons/s33`) 두 세대가 겹쳐 있고, 둘 다 한 번에 최종 주소로 보낸다(체인을 만들지 않는다).
- **홈 검색은 프로그램을 안 가린다.** 홈이 프로그램 목록이 되면서 사람 검색이 한 클릭 뒤로 밀릴 뻔했는데, "검색 없이 바로 찾는다"가 이 사이트의 존재 이유라 홈 인덱스에 전 프로그램을 담았다.
- **`Season.label` 을 `number` 로 바꿨다.** 프로그램마다 부르는 말이 달라서다 — `33기` 와 `시즌 4`. 라벨을 저장하면 언어 × 프로그램만큼 적어야 하므로 번호만 두고 사전이 만든다.
- **`alias` 를 "방송에서 불린 이름"으로 재정의했다.** 나는 솔로는 가명, 솔로지옥은 실명이 들어간다. 필드를 나누지 않은 건 화면이 하는 일이 같아서다. **다만 처리방침과 삭제 창구는 함께 고쳤다** — 그 문구들이 "가명"·"기수와 가명"을 전제하고 있어서, 실명으로 나오는 프로그램이 붙는 순간 사실과 달라진다(`effectiveDate` 도 옮겼다).
- **사이트 이름을 프로그램 중립으로 바꿨다**(`나는 솔로 출연진 인스타` → `누꼬 — 예능 출연진 인스타`). title.template·`og:site_name`·`WebSite` 이름·탐색경로 첫 칸이 다 그 값이라, 프로그램이 둘이 되는 순간 사이트가 자기 이름으로 거짓말을 하게 됐다.
- **솔로지옥은 시즌 1~5 골격만 넣었다**(2021-12 / 2022-12 / 2023-12 / 2025-01 / 2026-01). 명단을 짐작으로 채우면 없는 사람을 만들게 되므로 `cast` 는 빈 배열이고, 그래서 프로그램 화면째로 색인에서 빠져 있다(`isProgramIndexable`). 명단이 들어오면 저절로 돌아온다.
- **홈은 네 번 만에 자리를 잡았다.** ① 기수 목록과 같은 줄 문법 — 프로그램은 기수보다 위인데 줄로 그리니 오히려 가벼웠다. ② 전면 카드 — 화면은 찼지만 **프로그램이 늘면 홈이 스크롤 지옥이 된다**(로드맵대로면 프로그램은 계속 는다). ③ 아바타를 넣은 격자 타일 — 판 안이 UI 로 차서 투박했다. ④ **넷플릭스·티빙을 실제로 열어 보고** 그 문법으로 갔다: 포스터 한 장 + 판 밖의 현황 한 줄, 판 안에는 그림과 모서리 배지만.
- **판에는 프로그램 포스터를 건다**(§9 ① 의 "프로그램 포스터" 항). 처음엔 제작사 저작물이라 못 싣는다고 보고 이름을 활자로 앉히는 쪽으로 갔는데, 같은 날 뒤집었다 — 포스터를 싣고 사후 대응으로 간다. 포스터가 없으면 활자 판으로 넘어가므로 그때 만든 것도 그대로 산다. 포스터가 없는 동안 프로그램을 가르는 일은 색 대신 **플랫폼 캡션**(`SBS PLUS · ENA` / `NETFLIX`)이 진다 — 색면으로 가르는 건 §8 에서 반복 반려된 방향이다. 지금까지 읽는 곳이 없던 `Program.platform` 이 여기서 살아났다.
- 프리렌더는 117 → 138 페이지.

## 9. 유의사항

- 모두 **공개적으로 알려진 정보**(방송 출연 시 공개되었거나 본인이 공개 설정한 인스타 계정)만 정리한다.
- 비공개 계정이나 확인되지 않은 추정 계정은 올리지 않는다.
- 출연자가 삭제/비공개 전환을 요청하면 즉시 반영할 수 있는 절차를 마련한다. → **구현됨**: `/takedown` 창구와 `/privacy` 처리방침, 전 화면 푸터 링크. 요청을 반영하는 방법은 `src/data/README.md` 참고.

### 수익화하면 성격이 달라진다

광고를 붙이는 순간 "비영리 팬페이지"가 아니게 된다. 아래는 법률 자문이 아니라 **무엇을 전문가에게 물어봐야 하는지**에 대한 정리다. 본격적으로 돈을 붙이기 전에 한 번은 확인하는 게 맞다.

**① 사진 — 가장 큰 위험이다. 걷어냈다 되돌렸고, 2026-08-24 에 싣기로 정했다**

원래 시안 D는 "사진이 주인공"이었다. 그런데 그 사진의 출처가 둘 중 하나였다.

- **방송 캡처** → 저작권자는 제작사·방송사다. 영리 목적 복제·전송이고, 이 디자인에서 사진은 화면을 가득 채우는 주요소라 인용·부수적 이용 항변이 어렵다.
- **인스타 프로필 사진** → 저작권은 본인, 여기에 초상권이 겹친다.

게다가 출연자는 **연예인이 아니라 일반인**이다(가명으로 나오는 이유가 그것이다). 일반인의 얼굴을 영리 사이트에 싣는 것은 인격권 침해 주장이 가능한 영역이다.

**현재 방침: 올려 두고, 요청이 오면 내린다.** 2026-08-19 에 필드와 UI 를 통째로 없앴다가 2026-08-20 에 되돌렸다(§5·§8). 사전에 전부 허락을 받는 건 현실적으로 불가능하고, 그렇다고 사진을 아예 안 쓰면 시안 D 가 성립하지 않는다. 그래서 **사후 대응**으로 간다 — 싣되, 내려 달라는 말이 나오면 즉시 내린다.

이 방침이 성립하려면 **내리는 쪽이 실제로 빨라야 한다.** 그래서 세 가지를 붙여 뒀다.

- `/takedown` 과 `/privacy` 가 사진을 명시한다. "사진은 쓰지 않습니다" 라고 적어 두고 싣는 건 처리방침이 거짓말을 하는 것이라 특히 위험하다.
- 절차가 계정과 사진을 함께 덮는다 — `src/data/README.md` 의 "내려 달라는 요청이 오면". **계정만 내리고 사진을 두면 요청을 반만 처리한 것이다.**
- `profileImageUrl` 이 선택이라 지우는 것으로 끝난다. 파일 하나 지우고 필드 한 줄 지우면 화면이 알아서 가명 배지로 넘어간다 — 되돌리기가 싸야 약속을 지킬 수 있다.

**사후 대응이 덮지 못하는 것은 알고 가자.** 요청이 오기 전까지 실린 기간은 이 방침으로 지워지지 않고, 초상권·저작권은 "요청하면 내려 준다"가 면책이 되는 구조가 아니다(저작권 통지·삭제 절차와 달리 법정 세이프하버가 없다). 즉 이건 리스크를 없애는 선택이 아니라 **줄이면서 감수하는 선택**이다. 특히 아래 ② 대로 수익화하면 형량이 더 불리해지니, 광고를 붙이기 전에 여기부터 다시 볼 것.

**프로그램 포스터는 별개 판단이고, 2026-08-31 에 싣기로 했다.** 저작권자는 제작사·방송사다. 아래 출연자 사진과 방침은 같다 — 싣되 내려 달라는 말이 오면 내린다. **다만 아래 "방송 캡처는 안 쓴다" 규칙을 여기까지 넓히지 말 것.** 둘은 걸리는 권리가 다르다: 포스터는 저작권 하나고 요청 주체가 제작사 한 곳이지만, 출연자 사진은 그 위에 **일반인의 초상권**이 겹치고 요청 주체가 당사자 개인이다. 후자가 이 사이트의 실제 사고 유형(§9 ③)과 직결되는 쪽이라 규칙을 좁게 유지한다. 규격은 `src/data/README.md` 의 "프로그램 포스터를 올릴 때".

**2026-08-24 에 그 "다시 보기"를 했고, 결론은 싣는다는 쪽이다.** 대신 감수한 만큼 두 가지를 조였다.

- **출처를 본인 인스타 프로필 사진으로 한정한다. 방송 캡처는 안 쓴다.** 캡처는 저작권자가 제작사라 개인 요청과 달리 협상 여지가 없고, 한 번에 사이트 전체가 걸린다. 프로필 사진은 저작권·초상권이 모두 본인이라 요청 주체가 한 명이고, `/takedown` 이 설계된 그대로 작동한다 — 사후 대응이 성립하는 출처는 이쪽뿐이다. 규격과 절차는 `src/data/README.md` 의 "사진을 올릴 때".
- **광고를 사진보다 먼저 붙였다** — 같은 날 안에 순서가 뒤집혔다. 원래 판단은 "사후 대응이 실제로 작동하는지 보고 나서 영리성을 얹는다" 였는데, 실제로는 애드센스를 먼저 넣었다(아래 §10). 그때 사진이 0장이라 그 시점의 사이트에는 얼굴이 없었고, 심사와 색인이 몇 주 걸리는 동안 사진 작업이 병행되기 때문이다.

  **그래서 남는 것은 알고 가자.** 사진을 올리는 순간 영리성이 이미 붙어 있는 상태가 된다 — 그게 원래 미루려던 바로 그 조건이다. 순서가 바뀌었다고 위험이 준 게 아니라, 사진을 올릴 때 그 조건이라는 걸 알고 올리는 것으로 정리됐다.

**② 개인정보보호법** — 이름·사진·SNS 계정은 개인정보다. "이미 공개된 정보"는 면허가 아니고, 공개된 개인정보는 **이익형량**을 거쳐 처리 가능하다는 게 대체적 법리인데 **영리성은 형량에서 불리하게 작용**한다. 본인이 계정을 공개한 목적이 "아카이브에 실리기 위해서"는 아니라는 점도 방어가 쉽지 않다.

**③ 실질적으로 가장 자주 터지는 사고: 잘못된 계정.** 엉뚱한 사람이 DM 폭탄을 맞는다. 나솔 출연자는 방영 후 악플 대상이 되는 일이 잦아 삭제 요청은 대개 여기서 온다. 지금의 세 규칙(지어내지 않기 · `source` 필수 · 가명 우선)은 데이터 품질 규칙처럼 보이지만 **실제로는 법적 방어선**이다. 절대 느슨하게 하지 말 것. 검증 방법은 `src/data/README.md`.

**④ 애드센스** — 콘텐츠가 얇으면 심사에서 떨어진다(데이터 채우기가 곧 심사 준비다). 개인정보 처리방침 페이지가 필수고, 타인을 괴롭히는 콘텐츠 조항이 있어 ③의 사고가 나면 정책 위반으로 수익이 끊길 수 있다.

**⑤ Meta 약관** — 자동 수집(스크래핑)은 약관 위반이다. 사람이 검색해 확인하는 것과 다르다. 이미지 핫링크 금지 규칙은 지키고 있다.

**지금 당장 해 둘 것**: 삭제 요청 창구(이메일 한 줄)와 개인정보 처리방침 페이지. 사고 난 뒤에 붙이는 것보다 훨씬 싸고, 애드센스에도 어차피 필요하다.

## 10. 진행 상황

### 완료

- 프로젝트 세팅(Next.js 16 + Tailwind v4 + shadcn/ui, pnpm)
- 네 화면 — 기수 목록·기수 상세·삭제 요청(`/takedown`)·개인정보 처리방침(`/privacy`). 전 페이지 SSG 프리렌더
- SEO 배관 — `sitemap.ts`, `robots.ts`, `metadataBase`, canonical, 기수별 OG 이미지(코드 생성)
- Vercel 배포 — https://www.nukko.net (2026-08-24 커스텀 도메인 연결, Cloudflare Registrar). 도메인 값은 `lib/site.ts` 한 곳에서 정해진다 — Vercel 자동값이 "가장 짧은 커스텀 도메인"을 고르는 탓에 리다이렉트 전용 apex 가 뽑혀서, `NEXT_PUBLIC_SITE_URL` 로 못박았다
- 기수 골격 1~33기 (18~24기만 있던 것을 전 기수로 확장)
- **사진 제거 → 복원** — §9 ① 위험 때문에 2026-08-19 에 이미지 필드와 사진 UI 를 통째로 걷어냈다가, 2026-08-20 에 시안 D 의 이미지 카드로 되돌렸다. 이번엔 필드가 선택이라 사진이 없어도 가명 배지로 굴러간다(§5·§8). 되돌리면서 삭제 절차가 사진까지 덮게 함께 고쳤다 — `/privacy` 가 "사진은 쓰지 않습니다" 라고 적은 채였고, `/takedown` 은 계정만 받고 있었다. 실제 사진 파일은 아직 없다
- **삭제 요청 창구·개인정보 처리방침** — §9 가 약속만 해 두고 없던 절차를 실제로 붙였다. 연락처는 `lib/site.ts` 의 `CONTACT_EMAIL` 한 곳
- **라이트 모드** — 다크 전용으로 시작했다가 두 팔레트를 다 갖도록 바꿨다(§8). 기본값은 여전히 다크
- **검색** — 홈 기수 목록 위 검색창. 기수 번호·특집 이름·가명·핸들로 찾고, 결과에서 그 사람 카드로 바로 내려간다. 헤더 돋보기 + `⌘K` 팔레트로 먼저 만들었다가 목록 위 검색창으로 바꿨다. 가명이 21개뿐이라 한 단어 검색은 기수 수만큼 걸리므로(`영수` 33건), 계정을 찾아 둔 사람을 위로 올리고 `22기 영수` 처럼 기수를 함께 치면 좁혀지게 했다. 명단이 다 차면서 이제는 `MAX_MEMBERS`(24)에 실제로 걸려 잘린다 — 기수를 함께 치는 게 권장이 아니라 사실상 필수가 됐다
- **화면 문구를 존댓말로 통일** — 처음엔 사이트 전체가 평서체였다. 방문자에게 보이는 글은 `합니다`체, 코드 주석과 이 문서들은 평서체(`CLAUDE.md` 의 문구 규칙)
- **검색 노출 2차 손질**(2026-08-21) — 배관은 이미 있었고 남은 구멍 넷을 메웠다. ① 기수 상세 본문에 프로그램 이름이 한 글자도 없었다(제목은 "나는 솔로 33기…" 인데 화면엔 `33기` 뿐) ② 구조화 데이터(JSON-LD) 추가 — 홈은 `WebSite`, 나머지는 `BreadcrumbList`, 기수 상세는 계정을 찾아 둔 사람의 `ItemList` ③ 명단이 빈 7개 기수를 noindex + sitemap 제외 — soft 404 판정이 사이트 전체 평가로 번지는 걸 막는다 ④ 페이지가 `openGraph` 를 정의하면 레이아웃 것이 통째로 덮이는 Next 의 얕은 병합 때문에 기수·정책 페이지에서 `og:site_name`·`og:locale` 이 빠져 있었다. 판단은 전부 `lib/seo.ts` 한 곳. 배포 후 프로덕션에서 확인했다 — JSON-LD 가 실제 도메인으로 나가고(로컬 빌드에서는 `localhost:3000` 으로 박힌다), 세 페이지의 `og:site_name`·`og:locale` 이 살아났고, `s32` 에 `noindex, follow`, sitemap 은 72 → 58 줄
- **브랜드 워드마크·앱 아이콘** — [ㄲ 마크] `누꼬`(ko)·[ㄲ 마크] `nukko`(en). 마크는 언어를 안 타고 이름만 탄다 — 파비콘이 `[lang]` 바깥이라 언어를 못 받기 때문이다. 한글 워드마크를 반려했던 규칙은 `누꼬` 가 번역이 아니라 원문이라서 뒤집혔다. 네 화면 헤더가 같은 `‹ 제목` 구조로 통일됐다. **마크는 하루 만에 두 번 바뀌었다**(둘 다 2026-08-24) — 처음엔 `@whosthat` 에서 `@누꼬`/`@nukko` 로(인스타그램을 가리키는 `@`, Manrope 글리프를 그대로 그린 파비콘), 이어서 `@` 를 통째로 버리고 겹친 두 원의 둘레에서 ㄱ 두 개를 깎은 ㄲ 모노그램으로 바꿨다 — 회사 로고가 되려면 세계 공용 기호(`@`·원·물음표·말풍선 다 검토 후 기각)가 아니라 이름 속 글자에서 나온 도형이라야 남이 못 쓴다는 판단이었다. 도형이 되면서 파비콘·앱 아이콘의 폰트 로딩(Manrope 서브셋)이 통째로 필요 없어졌다
- **검색엔진 등록**(2026-08-24) — 커스텀 도메인이 붙은 뒤로 미뤄 뒀던 일. **Google Search Console** 은 도메인 속성 + DNS TXT 로 확인해서 코드에 남는 게 없다. **네이버 서치어드바이저**는 DNS 방식이 없어서 `lib/site.ts` 의 `NAVER_SITE_VERIFICATION` 을 루트 레이아웃 `metadata.verification` 에 걸었다. 둘 다 소유확인과 sitemap 제출까지 끝났다 — 구글은 발견된 페이지 72(프로덕션 실측과 일치), 네이버는 사이트 목록에 등록일과 함께 잡혔다. **Bing Webmaster Tools** 는 "Import from Google Search Console" 로 구글 쪽 소유확인을 그대로 물려받아서 코드도 새 값도 없다. sitemap 도 함께 넘어온 것을 Sitemaps 메뉴에서 확인했다. 색인은 이제 각 엔진이 알아서 돈다
- **광고 — 구글 애드센스**(2026-08-24) — §9 ④ 가 요건으로 꼽은 것(처리방침 페이지, 콘텐츠 분량)이 이미 갖춰져 있어서 코드는 하루면 됐다. **손이 많이 간 건 처리방침 쪽이다** — `visitor2` 가 "쿠키는 쓰지 않고 방문자를 따로 식별하지 않습니다" 라고 단언하고 있어서, 광고 스크립트가 들어가는 순간 거짓이 된다. 그 주장을 Vercel Analytics 로 한정하고, `processor` 에 Google LLC 를 제3자로 넣고, `ads` 를 실제 게재 상태로 다시 썼다(ko·en 두 벌, 시행일도 옮겼다). 커밋을 둘로 나눠 문구를 먼저 배포한 뒤 게시자 ID 를 채웠다 — 처리방침과 실제 상태가 어긋나는 구간이 생기지 않게. 값은 `lib/site.ts` 의 `ADSENSE_CLIENT_ID` 와 `public/ads.txt` 두 곳이 짝이고, 자동 광고라 배너 자리를 코드로 만들지 않았다. 운영 쪽은 소유권 확인(ads.txt 방식 — 코드 스니펫 방식은 apex 등록 탓인지 실패했다)·검토 요청 제출·GDPR 동의 메시지(구글 CMP, **거부 버튼이 첫 화면에 있는 3선택 형식**)까지 끝냈다. 미국 주 규정 메시지는 만들지 않았다 — CCPA 는 매출·이용자 수 기준이 있어 대상이 아니고, 없어도 구글이 광고를 막지 않는다(GDPR 과 다른 점이다). **남은 건 심사 결과를 기다리는 것뿐이다**
- **일본어**(2026-08-25) — §8-1 이 "다음 후보" 로 적어 뒀던 것. 문구와 어휘 표를 채우는 건 절차대로였고, **예상 밖이었던 건 셋이다.** ① 가타카나로 옮기니 `정수`·`종수` 가 둘 다 `ジョンス` 로 겹쳤다 — 1·2·3기는 그 둘이 같은 기수에 있어서 한 화면에 이름이 같은 사람이 둘 생긴다. 일본 매체가 두 표기를 다 쓰는 걸 확인하고 `정`→`チョン`·`종`→`ジョン` 으로 갈라 못박았다. ② **서체를 언어마다 따로 골라야 했다** — Gothic A1 에 가나·한자가 없어서 그대로 두면 일본어 화면이 통째로 시스템 폰트로 떨어진다. Zen Kaku Gothic New 로 갔고, 공유 카드가 화면과 다른 글꼴이 되지 않게 OG 이미지도 같은 서체로 짝을 맞췄다. ③ 언어가 셋이 되면서 헤더의 순환 토글이 성립하지 않아 드롭다운으로 바꿨다 — 둘일 땐 "누르면 다른 쪽" 이 자명했지만 셋부터는 눌러 봐야 안다. 워드마크는 `nukko` 로 뒀다: `누꼬` 가 원문이라 한국어가 예외인 것이지, 가타카나 음역까지 그 예외를 넓히면 언어마다 예외가 하나씩 생긴다. 프리렌더는 80 → 117 페이지

### 데이터 현황

**프로그램 2개.** 나는 솔로는 기수 33개 · 인원 408명이고 계정은 `found` 320 · `none` 1 · `searching` 87 이다(2026-08-24 기준). 솔로지옥은 시즌 5개 · 인원 0명 — 골격만 있고 명단이 비어 있다(2026-08-31).

**이 숫자는 스냅샷이고 정답은 데이터 파일이다.** 뽑는 법은 `src/data/README.md` 맨 앞에 있다 — 여기 적힌 값이 실제와 어긋나면 데이터 쪽이 맞다.

**명단을 비워 둔 기수는 이제 없다.** 1~5기(초기 가명 체계)·25기(미경 대체자 불명)·32기(인원 자료 불일치)가 오래 비어 있었지만 2026-08-21 에 전부 채웠다.

그래서 화면의 "명단 정리 중"과 거기 딸린 noindex·sitemap 제외에 지금 걸리는 기수는 하나도 없다. 장치를 걷어낸 게 아니라 해당자가 없는 것이다 — `lib/seo.ts` 의 `isIndexable` 은 그대로 살아 있고, 새 기수를 골격만 먼저 넣으면 다시 작동한다.

### 계정 채우기 — 다음 배치 순서

확보율이 기수 성격에 크게 좌우된다(§ `src/data/README.md` 의 검증 방법). **돌싱·40대 특집이 수확이 좋다** — 출연자가 사업·강사·유튜브를 하고 있어 공개 계정에 기수를 내걸어 둔다.

1. ~~**27 · 26 · 24기**~~ — 한 바퀴 돌렸다. 27기는 종영이 얼마 안 돼 인사 게시물이 덜 올라왔고, 근거가 커뮤니티 추측 수준인 후보가 많아 대부분 `searching` 으로 남겼다.
2. ~~**22기(3차 돌싱)**~~ — 다 채웠다. 남은 특집은 **7기(40대) 6 · 14기(2차 40대) 6 · 10기(1차 돌싱) 2 · 16기(2차 돌싱) 1** 명이다
3. 나머지 정규 기수 — 잔여가 많은 순서는 **25기 6 · 3기 5 · 9기 5 · 12기 5** 다
4. **33기(12명 전원)는 종영(10월 예정) 후** — 방영 중엔 계정이 잠겨 있어 채울 수 없다

25기는 명단이 안 잡혀서 오래 조사 대상에서 빠져 있었지만, 명단이 채워지면서 이제 대상이다 — 6명이 `searching` 이라 33기 다음으로 잔여가 많다.

### 그 다음

~~삭제 요청 창구·개인정보 처리방침~~ (구현됨 — §9) → ~~검색 기능~~ (구현됨 — 목록 위 검색창) → ~~검색엔진 등록~~ (구글·네이버·Bing 세 곳 다 완료 — 위 "완료" 참고) → ~~광고~~ (애드센스 심사 대기 — 위 "완료") → ~~일본어~~ (구현됨 — 위 "완료", §8-1) → ~~프로그램 확장 구조~~ (구현됨 — §8-2) → **솔로지옥 명단 채우기** → 사진 채우기(파이프라인은 붙었고 파일이 0장이다) → 제보 폼(로드맵 2단계)

검색엔진 등록을 커스텀 도메인 연결 뒤로 미뤄 둔 이유는 검증과 색인이 **호스트네임 단위로** 쌓이기 때문이었다 — `.vercel.app` 으로 먼저 등록했으면 도메인을 옮길 때 검증·sitemap 제출·색인을 통째로 다시 했을 것이다. 한국어 쿼리로 안 뜨던 것도 온페이지 문제가 아니라 단순 미등록이었다(`site:www.nukko.net` 으로 확인했었다).

커스텀 도메인은 2026-08-24 에 연결했고, 절차는 `CLAUDE.md` 의 "커스텀 도메인을 연결할 때"에 있다. 요점은 **코드를 고칠 게 없다는 것** — `lib/site.ts` 한 곳이 도메인을 정한다.

다만 **"Primary 도메인 지정" 같은 설정은 Vercel 에 없다.** 자동값(`VERCEL_PROJECT_PRODUCTION_URL`)이 "가장 짧은 커스텀 도메인"을 고르는 탓에 리다이렉트 전용인 apex 가 뽑히므로, `NEXT_PUBLIC_SITE_URL` 로 못박는 게 맞다. sitemap 의 `<loc>` 은 프로덕션에서 확인했다(전부 `https://www.nukko.net`).

**남은 건 색인을 기다리는 것뿐이다.** 등록·sitemap 제출은 운영이 끝냈고 이제부터는 각 엔진의 크롤링 속도에 달렸다 — 며칠에서 몇 주 걸릴 수 있다. `site:www.nukko.net` 으로 가끔 진행 상황을 확인하면 된다.
